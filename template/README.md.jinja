# {{ project_name }} GitOps

GitOps configuration for `{{ project_name }}` managed with **{{ gitops_tool | upper }}**.

## Environments

{% for env in environments %}
- `{{ env }}`
{% endfor %}

## Bootstrap

{% if gitops_tool == 'argocd' %}
```bash
kubectl create namespace {{ argocd_namespace }}
kubectl apply -n {{ argocd_namespace }} -f bootstrap/argocd/install.yaml
kubectl apply -n {{ argocd_namespace }} -f bootstrap/argocd/app-of-apps.yaml
```
{% else %}
```bash
flux bootstrap github \
  --owner=<your-github-user> \
  --repository={{ project_name }}-gitops \
  --branch=main \
  --path=clusters/dev
```
{% endif %}
